interface:
  display_name: "Code Review Loop"
  short_description: "Pre-handoff review/fix loop until stable"
  default_prompt: "Use $code-review-loop before declaring implementation done: run the risk-tiered revision-bound worker review, fix findings, validate, and rerun only invalidated layers until the configured target is clean."

policy:
  allow_implicit_invocation: true
