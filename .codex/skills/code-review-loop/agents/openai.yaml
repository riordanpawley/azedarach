interface:
  display_name: "Code Review Loop"
  short_description: "Iterative review/fix loop until findings stabilize"
  default_prompt: "Use $code-review-loop to review this change, fix actionable findings, and repeat until repeated passes find no new issues."
