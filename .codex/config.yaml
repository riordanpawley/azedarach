permission:
  bash:
    "gh": allow
    "gh *": allow
    "git add": allow
    "git add *": allow
    "git commit": allow
    "git commit *": allow
    "git push": allow
    "git push *": allow
    "git -C * add": allow
    "git -C * add *": allow
    "git -C * commit": allow
    "git -C * commit *": allow
    "git -C * push": allow
    "git -C * push *": allow
