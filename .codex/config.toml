model = "gpt-5.3-codex"
model_reasoning_effort = "medium"

# Closest Codex equivalents to OpenCode runtime behavior.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
multi_agent = true
