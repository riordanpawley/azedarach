{
	"$schema": "https://biomejs.dev/schemas/2.3.8/schema.json",
	"vcs": {
		"enabled": true,
		"clientKind": "git",
		"useIgnoreFile": true
	},
	"assist": {
		"actions": {
			"source": {
				"organizeImports": "on"
			}
		}
	},
	"linter": {
		"enabled": true,
		"rules": {
			"recommended": true,
			"correctness": {
				"noUnusedImports": "warn",
				"noUnusedVariables": "warn",
				"useImportExtensions": {
					"level": "error",
					"options": {
						"forceJsExtensions": true
					}
				}
			},
			"style": {
				"noNonNullAssertion": "off"
			},
			"suspicious": {
				"noExplicitAny": "warn"
			}
		}
	},
	"overrides": [
		{
			"includes": ["src/**", "bin/**"],
			"linter": {
				"rules": {
					"style": {
						"noRestrictedImports": {
							"level": "error",
							"options": {
								"paths": {
									"@azedarach/daemon": "Import workspace packages through the local source tree or the public facade instead of a package alias.",
									"@azedarach/tui": "Import workspace packages through the local source tree or the public facade instead of a package alias."
								}
							}
						}
					}
				}
			}
		},
		{
			"includes": ["packages/**"],
			"linter": {
				"rules": {
					"style": {
						"noRestrictedImports": {
							"level": "error",
							"options": {
								"paths": {
									"@azedarach/cli": "Import sibling package source through the public facade or the local package source instead of a package alias.",
									"@azedarach/daemon": "Import sibling package source through the public facade or the local package source instead of a package alias.",
									"@azedarach/tui": "Import sibling package source through the public facade or the local package source instead of a package alias.",
									"packages/cli/src": "Import sibling package source through the public facade or the local package source instead of a direct package path.",
									"packages/daemon/src": "Import sibling package source through the public facade or the local package source instead of a direct package path.",
									"packages/tui/src": "Import sibling package source through the public facade or the local package source instead of a direct package path."
								}
							}
						}
					}
				}
			}
		}
	],
	"formatter": {
		"enabled": true,
		"indentStyle": "tab",
		"indentWidth": 2,
		"lineWidth": 100
	},
	"javascript": {
		"formatter": {
			"quoteStyle": "double",
			"semicolons": "asNeeded"
		}
	},
	"files": {
		"includes": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.json"]
	}
}
