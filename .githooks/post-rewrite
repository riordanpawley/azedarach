#!/usr/bin/env sh
# >>> azedarach managed githook >>>
az githooks hook --hook post-rewrite "$@" >/dev/null 2>&1 || true
# <<< azedarach managed githook <<<

set -eu

# Canonical validation is launched explicitly by integration orchestration only
# after rebase has returned and removed its reconciliation state.
