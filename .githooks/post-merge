#!/usr/bin/env sh
# >>> azedarach managed githook >>>
az githooks hook --hook post-merge "$@" >/dev/null 2>&1 || true
# <<< azedarach managed githook <<<

set -eu
# Auto-generation was removed after migration to native Codex context files.
# Canonical validation is launched explicitly by integration orchestration only
# after reconciliation has returned an exact clean candidate HEAD.
exit 0
