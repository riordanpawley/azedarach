#!/usr/bin/env sh
set -eu

# Candidate validation is launched explicitly after rebase returns an exact
# clean HEAD. Running it here validates the pre-rebase commit and can leave an
# obsolete aggregate alive while conflict reconciliation proceeds.
exit 0
