#!/usr/bin/env sh
set -eu

if [ "${RULESYNC_SKIP:-0}" = "1" ]; then
  printf '%s\n' "rulesync: skipped (RULESYNC_SKIP=1)"
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
"$repo_root/scripts/rulesync-sync.sh" sync >/dev/null
