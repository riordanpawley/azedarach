# az spec Phase 1

Generated by `az spec export --target md`.

## Current Runtime Status

- CLI grammar is restored for `req`, `link`, `read`, `lint`, `parity`, and `export --target md`.
- `az spec export --target md` is implemented in the Go CLI layer and keeps this directory deterministic.
- `req`, `link`, `read`, `lint`, and `parity` currently validate syntax and return a consistent not-implemented error until daemon-backed spec contracts land.

## Requirement Commands

1. `az spec req list [--json] [--issue <issue-id>] [--status <open|accepted|superseded>] [--id <req-id> ...] [--ids a,b,c]`
2. `az spec req get --id <req-id> [--json]`
3. `az spec req create --id <req-id> --title <text> [--description <text>] [--issue <issue-id>] [--json]`
4. `az spec req update --id <req-id> [--title <text>] [--description <text>] [--status <open|accepted|superseded>] [--json]`
5. `az spec req delete --id <req-id> --confirm [--json]`

## Link Commands

1. `az spec link list [--json] [--issue <issue-id>] [--req <req-id>] [--id <link-id> ...] [--ids a,b,c]`
2. `az spec link add --issue <issue-id> --req <req-id> [--role <implements|verifies|relates>] [--note <text>] [--json]`
3. `az spec link remove --issue <issue-id> --req <req-id> [--json]`

## Read, Lint, Parity, and Export

1. `az spec read [--json] [--issue <issue-id>] [--req <req-id>]`
2. `az spec lint [--json] [--strict]`
3. `az spec parity [--json] [--fail-on-out]`
4. `az spec export --target md [--check] [--json]`

## Determinism Rules

- Flags stay before positional arguments.
- Repeated selector flags preserve caller order after first-occurrence deduplication.
- `--ids` comma lists trim whitespace and drop empty tokens.
- Disabled spec workflows fail fast with remediation: `az config set spec.enabled true`.
- `az spec export --target md --check` reports drift without mutating files.
